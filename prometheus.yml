global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - "alerting_rules.yml"

scrape_configs:
  - job_name: "jobqueue"
    static_configs:
      - targets: ["metrics-exporter:8000"]
    metrics_path: /metrics
